# Установка MongoDB на Windows для MemoAI

## 📋 Содержание
1. [Вариант 1: Установка MongoDB Community Server (рекомендуется)](#вариант-1-установка-mongodb-community-server)
2. [Вариант 2: Использование Docker Desktop](#вариант-2-использование-docker-desktop)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Проверка подключения](#проверка-подключения)
5. [Решение проблем](#решение-проблем)

---

## Вариант 1: Установка MongoDB Community Server (рекомендуется)

### Шаг 1: Скачивание MongoDB

1. Перейдите на официальный сайт MongoDB:
   ```
   https://www.mongodb.com/try/download/community
   ```

2. Выберите:
   - **Version**: `7.0.x` (последняя стабильная версия)
   - **Platform**: `Windows`
   - **Package**: `msi`

3. Нажмите **Download** и дождитесь завершения загрузки

### Шаг 2: Установка MongoDB

1. Запустите скачанный `.msi` файл (например, `mongodb-windows-x86_64-7.0.x.msi`)

2. В мастере установки:
   - Нажмите **Next**
   - Примите лицензионное соглашение (**I accept the terms**)
   - Выберите **Complete** (полная установка)

3. **Service Configuration** (важно!):
   - ✅ Оставьте галочку **Install MongoDB as a Service**
   - Service Name: `MongoDB`
   - Data Directory: `C:\Program Files\MongoDB\Server\7.0\data\`
   - Log Directory: `C:\Program Files\MongoDB\Server\7.0\log\`
   - ✅ Оставьте галочку **Run service as Network Service user**

4. **Install MongoDB Compass** (опционально):
   - ✅ Можете оставить галочку для установки графического интерфейса
   - Или снимите галочку, если не нужен GUI

5. Нажмите **Install** и дождитесь завершения установки

6. Нажмите **Finish**

### Шаг 3: Проверка установки

1. Откройте **PowerShell** или **Command Prompt** от имени администратора

2. Проверьте, что MongoDB запущен как служба:
   ```powershell
   Get-Service MongoDB
   ```
   
   Должно показать:
   ```
   Status   Name               DisplayName
   ------   ----               -----------
   Running  MongoDB            MongoDB Server
   ```

3. Проверьте версию MongoDB:
   ```powershell
   mongod --version
   ```

4. Подключитесь к MongoDB Shell:
   ```powershell
   mongosh
   ```
   
   Если успешно, увидите:
   ```
   Current Mongosh Log ID: ...
   Connecting to: mongodb://127.0.0.1:27017/
   Using MongoDB: 7.0.x
   ```

5. Выйдите из shell:
   ```
   exit
   ```

### Шаг 4: Добавление MongoDB в PATH (если не добавлено автоматически)

1. Нажмите **Win + X** → **System**
2. Нажмите **Advanced system settings**
3. Нажмите **Environment Variables**
4. В разделе **System variables** найдите **Path** и нажмите **Edit**
5. Нажмите **New** и добавьте:
   ```
   C:\Program Files\MongoDB\Server\7.0\bin
   ```
6. Нажмите **OK** во всех окнах
7. Перезапустите PowerShell/CMD

---

## Вариант 2: Использование Docker Desktop

Если у вас уже установлен Docker Desktop:

### Шаг 1: Запуск MongoDB через Docker Compose

Проект уже содержит настроенный `docker-compose.yml`. Просто запустите:

```powershell
# Перейдите в директорию проекта
cd F:\memo_new_api

# Запустите только MongoDB
docker-compose up -d mongodb
```

### Шаг 2: Проверка

```powershell
# Проверьте, что контейнер запущен
docker ps

# Должен показать контейнер memoai-mongodb
```

### Шаг 3: Подключение к MongoDB в Docker

```powershell
# Подключитесь к MongoDB Shell в контейнере
docker exec -it memoai-mongodb mongosh -u admin -p password
```

---

## Настройка переменных окружения

### Для локальной установки MongoDB (Вариант 1):

Создайте файл `.env` в корне проекта (`F:\memo_new_api\.env`):

```env
# MongoDB настройки (локальная установка)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=memoai
MONGODB_USER=admin
MONGODB_PASSWORD=password
```

### Для Docker (Вариант 2):

```env
# MongoDB настройки (Docker)
MONGODB_HOST=mongodb
MONGODB_PORT=27017
MONGODB_DATABASE=memoai
MONGODB_USER=admin
MONGODB_PASSWORD=password
```

---

## Проверка подключения

### Тест 1: Подключение через Python

Создайте тестовый скрипт `test_mongodb.py`:

```python
import asyncio
from motor.motor_asyncio import AsyncIOMotorClient

async def test_connection():
    try:
        # Подключение к MongoDB
        client = AsyncIOMotorClient("mongodb://admin:password@localhost:27017/")
        
        # Проверка подключения
        await client.admin.command('ping')
        print("✅ Успешное подключение к MongoDB!")
        
        # Получение списка баз данных
        dbs = await client.list_database_names()
        print(f"📂 Доступные базы данных: {dbs}")
        
        # Создание тестовой коллекции
        db = client['memoai']
        collection = db['test']
        
        # Вставка тестового документа
        result = await collection.insert_one({"test": "Hello MongoDB!"})
        print(f"✅ Тестовый документ создан: {result.inserted_id}")
        
        # Чтение документа
        doc = await collection.find_one({"test": "Hello MongoDB!"})
        print(f"📄 Прочитан документ: {doc}")
        
        # Удаление тестового документа
        await collection.delete_one({"_id": result.inserted_id})
        print("✅ Тестовый документ удален")
        
        client.close()
        
    except Exception as e:
        print(f"❌ Ошибка подключения: {e}")

if __name__ == "__main__":
    asyncio.run(test_connection())
```

Запустите:
```powershell
python test_mongodb.py
```

### Тест 2: Через MongoDB Compass (если установлен)

1. Откройте **MongoDB Compass**
2. В поле **Connection String** введите:
   ```
   mongodb://localhost:27017/
   ```
3. Нажмите **Connect**
4. Вы должны увидеть список баз данных

---

## Решение проблем

### Проблема 1: MongoDB не запускается

**Решение:**
```powershell
# Остановите службу
net stop MongoDB

# Запустите заново
net start MongoDB

# Проверьте статус
Get-Service MongoDB
```

### Проблема 2: Порт 27017 занят

**Проверка:**
```powershell
netstat -ano | findstr :27017
```

**Решение:**
- Закройте программу, использующую порт
- Или измените порт MongoDB в конфигурации

### Проблема 3: Ошибка доступа

**Решение:**
```powershell
# Запустите PowerShell от имени администратора
# Создайте пользователя в MongoDB

mongosh

use admin
db.createUser({
  user: "admin",
  pwd: "password",
  roles: [ { role: "root", db: "admin" } ]
})
exit
```

### Проблема 4: "motor" не установлен

**Решение:**
```powershell
# Установите зависимости
pip install motor pymongo
```

---

## Запуск проекта с MongoDB

После установки и настройки MongoDB:

### Вариант 1: Локальный запуск

```powershell
# Убедитесь, что MongoDB запущен
Get-Service MongoDB

# Активируйте виртуальное окружение
.\venv_312\Scripts\activate

# Установите зависимости (если еще не установлены)
pip install -r backend/requirements.txt

# Запустите backend
cd backend
python main.py
```

### Вариант 2: Docker запуск

```powershell
# Запустите все сервисы
docker-compose up -d

# Проверьте логи
docker-compose logs -f memoai-backend
```

---

## Полезные команды MongoDB

### Через mongosh (MongoDB Shell):

```javascript
// Показать все базы данных
show dbs

// Использовать базу memoai
use memoai

// Показать все коллекции
show collections

// Найти все диалоги
db.conversations.find().pretty()

// Подсчитать количество диалогов
db.conversations.countDocuments()

// Удалить все диалоги (осторожно!)
db.conversations.deleteMany({})

// Создать индекс
db.conversations.createIndex({ "conversation_id": 1 })

// Показать индексы
db.conversations.getIndexes()
```

---

## Мониторинг MongoDB

### MongoDB Compass (GUI):
- Удобный графический интерфейс
- Просмотр данных в реальном времени
- Создание запросов
- Анализ производительности

### Через код:
```python
# Проверка здоровья
await client.admin.command('ping')

# Статистика базы данных
stats = await db.command('dbStats')
print(stats)
```

---

## Дополнительные ресурсы

- [Официальная документация MongoDB](https://www.mongodb.com/docs/)
- [MongoDB University (бесплатные курсы)](https://university.mongodb.com/)
- [Motor Documentation (async Python driver)](https://motor.readthedocs.io/)

---

## Готово! 🎉

Теперь MongoDB настроен и готов к работе с MemoAI!

При возникновении проблем проверьте:
1. ✅ MongoDB служба запущена
2. ✅ Порт 27017 доступен
3. ✅ Переменные окружения настроены
4. ✅ Зависимости Python установлены




