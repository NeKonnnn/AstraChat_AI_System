# 🚀 Быстрый старт PostgreSQL + pgvector для MemoAI

## Вариант 1: Docker (РЕКОМЕНДУЕТСЯ - 2 минуты) ⭐

### 1. Добавьте сервис PostgreSQL в `docker-compose.yml`

Сервис уже добавлен в docker-compose.yml (см. ниже).

### 2. Запустите PostgreSQL с pgvector

```powershell
docker-compose up -d postgresql
```

### 3. Проверьте подключение

```powershell
docker ps | findstr postgresql
```

### 4. Готово! ✅

Расширение pgvector уже установлено в Docker-образе `pgvector/pgvector:pg17`.

---

## Вариант 2: Локальная установка на Windows (15-30 минут)

### Шаг 1: Установите Visual Studio Build Tools

1. Скачайте **Visual Studio Build Tools**:
   - https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022
   - Выберите "Build Tools for Visual Studio 2022"
   - При установке выберите:
     - ✅ **C++ build tools**
     - ✅ **Windows 10/11 SDK**
     - ✅ **CMake tools for Windows**

### Шаг 2: Установите Git

Если еще не установлен:
- https://git-scm.com/download/win

### Шаг 3: Соберите pgvector из исходников

**Важно:** PostgreSQL у вас установлен в `E:\PostgreSQL\` (не в подпапке 17). 
pgvector можно клонировать в **любую папку** (например, `C:\temp\` или `E:\PostgreSQL\`).

Откройте **PowerShell** или **Command Prompt** (от имени администратора):

```powershell
# Создайте временную папку для сборки (или используйте любую другую)
cd C:\temp

# Клонируйте репозиторий pgvector
git clone --branch v0.7.3 https://github.com/pgvector/pgvector.git
cd pgvector

# Укажите путь к pg_config (ваш PostgreSQL установлен в E:\PostgreSQL\)
set "PG_CONFIG=E:\PostgreSQL\bin\pg_config.exe"

# Соберите расширение
make
make install
```

**После сборки файлы автоматически установятся в:**
- `E:\PostgreSQL\lib\vector.dll` - библиотека
- `E:\PostgreSQL\share\extension\vector*` - файлы расширения

**Если команда `make` не найдена:**
- Установите **MinGW-w64** или используйте **Visual Studio Developer Command Prompt**
- Или используйте **nmake** вместо **make**:

```powershell
# Откройте "x64 Native Tools Command Prompt for VS 2022"
# (из меню Пуск → Visual Studio 2022)

cd C:\temp\pgvector
set "PG_CONFIG=E:\PostgreSQL\bin\pg_config.exe"
nmake /F Makefile.win
nmake /F Makefile.win install
```

### Шаг 4: Установите расширение в базу данных

1. Откройте **pgAdmin4**
2. Подключитесь к серверу PostgreSQL
3. Выберите базу данных (или создайте новую `memoai`)
4. Откройте **Query Tool** (F5)
5. Выполните SQL:

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

### Шаг 5: Проверьте установку

```sql
-- Проверка версии PostgreSQL
SELECT version();

-- Проверка установленных расширений
SELECT * FROM pg_extension WHERE extname = 'vector';

-- Тест создания векторного типа
SELECT '[1,2,3]'::vector;
```

---

## Вариант 3: Готовые бинарники (если доступны)

Иногда можно найти готовые бинарники для Windows:
- Проверьте: https://github.com/pgvector/pgvector/releases
- Ищите файлы `.dll` для вашей версии PostgreSQL

---

## Проверка установки

### Через pgAdmin4:

1. Подключитесь к базе данных
2. Выполните SQL:

```sql
-- Проверка расширения
SELECT extname, extversion 
FROM pg_extension 
WHERE extname = 'vector';

-- Тест векторного типа
SELECT '[1,2,3]'::vector AS test_vector;
```

### Через Python (автоматическая проверка):

При запуске backend автоматически проверит наличие pgvector:

```powershell
cd backend
python main.py
```

В логах вы увидите:
- ✅ `Расширение pgvector успешно установлено` - если все ОК
- ❌ `Не удалось установить pgvector` - если есть проблемы

---

## Настройка переменных окружения

Создайте или обновите `.env` файл:

```env
# PostgreSQL + pgvector - для RAG системы
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=memoai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ваш_пароль
# Размерность векторов для эмбеддингов (по умолчанию 384 для multilingual-MiniLM)
EMBEDDING_DIM=384
```

---

## Проблемы и решения

### Ошибка: "make: command not found"

**Решение:** Используйте Visual Studio Developer Command Prompt или установите MinGW-w64.

### Ошибка: "pg_config: command not found"

**Решение:** Укажите полный путь (ваш PostgreSQL в `E:\PostgreSQL\`):
```powershell
set "PG_CONFIG=E:\PostgreSQL\bin\pg_config.exe"
```

### Ошибка: "extension vector does not exist"

**Решение:** 
1. Убедитесь, что pgvector собран и установлен
2. Проверьте, что файлы находятся в:
   - `E:\PostgreSQL\lib\vector.dll` - библиотека
   - `E:\PostgreSQL\share\extension\vector.control` - файл расширения
3. Перезапустите PostgreSQL сервис:
```powershell
Restart-Service postgresql-x64-17
```

### Ошибка при компиляции

**Решение:**
- Убедитесь, что установлены все зависимости Visual Studio Build Tools
- Проверьте версию PostgreSQL (должна быть 11+)
- Попробуйте более старую версию pgvector (например, v0.6.0)

---

## Рекомендация

**Для разработки:** Используйте Docker вариант - он самый простой и быстрый.

**Для продакшена:** Используйте локальную установку или Docker, в зависимости от вашей инфраструктуры.

---

## Готово! 🎉

Теперь MemoAI может использовать PostgreSQL + pgvector для RAG системы!

