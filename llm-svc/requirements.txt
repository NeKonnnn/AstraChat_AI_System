# -- Web framework 
fastapi==0.116.0
starlette==0.46.2
httpx==0.28.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
pydantic-settings>=2.0.0
python-dotenv==1.0.0
requests==2.32.4
tqdm==4.67.1
pyyaml==6.0.2
anyio==4.10.0
python-multipart>=0.0.18

# -- Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-ldap==3.4.4

# LLM зависимости
llama-cpp-python>=0.3.16

# vllm не в этом файле: только Dockerfile.cuda (иначе тяжёлая CPU-сборка и конфликты torch).

# Тесты: корневый requirements-dev.txt (pytest, pytest-asyncio)

# Ставятся отдельно в Dockerfile:
# torch, torchaudio - через pytorch.org index
# llama-cpp-python - через abetlen.github.io index
# vllm - только в Dockerfile.cuda, требует CUDA