# Инструкция по переносу файлов в микросервисные репозитории

Этот документ описывает, какие файлы и директории из корневой директории проекта нужно перенести в каждый из трех репозиториев микросервисов.

## Структура репозиториев

Проект разделяется на 3 независимых репозитория:
- **frontend** - React фронтенд приложение
- **backend** - FastAPI бэкенд с базами данных
- **llm_svc** - LLM сервис для обработки моделей

---

## 📁 Репозиторий: `frontend`

### Директории (полностью переносятся):
```
frontend/                    # Вся директория фронтенда
├── src/                     # Исходный код React
├── public/                  # Публичные файлы
├── build/                   # Собранное приложение (опционально)
├── node_modules/            # Зависимости (не коммитить, только package.json)
├── Dockerfile               # Docker образ для фронтенда
├── nginx.conf               # Nginx конфигурация для фронтенда
├── package.json             # Зависимости Node.js
├── package-lock.json        # Зафиксированные версии
├── tsconfig.json            # TypeScript конфигурация
└── README.md                # Документация фронтенда
```

### Файлы из корневой директории:
```
docker-compose-frontend.yml  # Docker Compose для фронтенда
assets/                      # Статические ресурсы (логотипы, иконки)
├── Astra_logo.png
├── copy_2.png
├── copy.png
├── icon.ico
└── logo.png
```

### Опциональные файлы (если используются):
```
nginx.conf                   # Если нужна отдельная конфигурация nginx для фронтенда
.env.example                 # Пример переменных окружения для фронтенда
.gitignore                   # Git ignore правила (или используйте frontend/.gitignore)
```

### Файлы, которые НЕ переносятся:
- `backend/` - это другой репозиторий
- `llm-svc/` - это другой репозиторий
- `models/` - модели не нужны фронтенду
- `docker-compose-backend.yml` - другой сервис
- `docker-compose-llm-svc.yml` - другой сервис
- `docker-compose.yml` - монолитный файл, не нужен
- Все Python файлы
- Все базы данных и их данные

---

## 📁 Репозиторий: `backend`

### Директории (полностью переносятся):
```
backend/                     # Вся директория бэкенда
├── agents/                  # Агенты
├── auth/                    # Аутентификация
├── config/                  # Конфигурация
├── database/                # Работа с БД (MongoDB, PostgreSQL, MinIO)
│   ├── mongodb/
│   ├── postgresql/
│   └── minio/
├── tools/                   # Инструменты
├── utils/                   # Утилиты
├── orchestrator/            # Оркестратор
├── memory/                  # Память (пустая директория для монтирования)
├── uploads/                 # Загруженные файлы (пустая директория)
├── Dockerfile               # Docker образ для бэкенда
├── requirements.txt         # Python зависимости
├── env.example              # Пример переменных окружения
├── README.md                # Документация бэкенда
└── ...                      # Все остальные файлы бэкенда
```

### Файлы из корневой директории:
```
docker-compose-backend.yml   # Docker Compose для бэкенда и БД
nginx.conf                   # Nginx конфигурация (используется в docker-compose)
install_pgvector_extension.sql  # SQL скрипт для установки pgvector
context_prompts.json         # Контекстные промпты (если используется бэкендом)
```

### Директории для данных (создать пустые или с .gitkeep):
```
memory/                      # Директория для памяти (монтируется в Docker)
uploads/                     # Директория для загруженных файлов
```

### Опциональные файлы:
```
models/                      # Только модели эмбеддингов (paraphrase-multilingual-MiniLM-L12-v2)
                             # Остальные модели (LLM, Vosk, Silero и т.д.) идут в llm_svc
.env.example                 # Пример переменных окружения
.gitignore                   # Git ignore правила
README/                      # Документация (если относится к бэкенду)
├── QUICK_START_MONGODB.md
├── QUICK_START_POSTGRESQL_PGVECTOR.md
├── QUICK_START_MINIO.md
└── ...                      # Другие релевантные README файлы
```

### Файлы, которые НЕ переносятся:
- `frontend/` - это другой репозиторий
- `llm-svc/` - это другой репозиторий
- `model_small/` - модели Vosk идут в llm_svc
- `silero_models/` - модели Silero идут в llm_svc
- `whisperx_models/` - модели WhisperX идут в llm_svc
- `diarize_models/` - модели диаризации идут в llm_svc
- Большие LLM модели из `models/` - идут в llm_svc
- `docker-compose-frontend.yml` - другой сервис
- `docker-compose-llm-svc.yml` - другой сервис

---

## 📁 Репозиторий: `llm_svc`

### Директории (полностью переносятся):
```
llm-svc/                     # Вся директория LLM сервиса
├── app/                     # Исходный код приложения
│   ├── api/                 # API endpoints
│   ├── core/                # Ядро приложения
│   ├── dependencies/        # Зависимости (Vosk, Silero, WhisperX и т.д.)
│   ├── services/            # Сервисы (LLM обработка)
│   └── utils/               # Утилиты
├── config/                  # Конфигурация
│   ├── config.yml
│   └── config.local.yml
├── models/                  # Скрипты для загрузки моделей
├── test/                    # Тесты
├── Dockerfile               # Docker образ для LLM сервиса
├── requirements.txt         # Python зависимости
├── env.example              # Пример переменных окружения
├── LICENSE                  # Лицензия
├── README.md                # Документация LLM сервиса
└── .gitignore               # Git ignore правила
```

### Файлы из корневой директории:
```
docker-compose-llm-svc.yml   # Docker Compose для LLM сервиса
```

### Директории с моделями (полностью переносятся):
```
models/                      # LLM модели (Qwen, CodeLlama, DeepSeek и т.д.)
├── Qwen_Qwen3-VL-30B-A3B-Instruct-Q6_K_L.gguf
├── Qwen3-Coder-30B-A3B-Instruct-Q8_0.gguf
├── Qwen3-VL-30B-A3B-Instruct-Q8_0.gguf
├── codellama-13b-instruct.Q8_0.gguf
├── codellama-7b-instruct.Q8_0.gguf
├── deepseek-coder-6.7b-instruct.Q8_0.gguf
├── DeepSeek-R1-Distill-Qwen-7B-Q8_0.gguf
├── granite-20b-code-instruct-Q8_0.gguf
├── Mistral-7B-Instruct-v0.3.Q8_0.gguf
├── starcoder2-15b-instruct-v0.1-Q8_0.gguf
├── starcoder2-7b-instruct-Q8_0.gguf
├── surya/                   # Модели Surya для OCR
└── ...                      # Все остальные LLM модели

model_small/                 # Модели Vosk для распознавания речи
├── am/
├── conf/
├── graph/
├── ivector/
└── README

silero_models/               # Модели Silero для синтеза речи
├── en/
└── ru/

whisperx_models/             # Модели WhisperX для транскрибации
└── ...                      # Все файлы WhisperX

diarize_models/              # Модели для диаризации (разделения спикеров)
├── models/
├── models--pyannote--speaker-diarization-3.1/
└── pyannote_diarization_config.yaml
```

### Опциональные файлы:
```
.env.example                 # Пример переменных окружения
README/                      # Документация (если относится к LLM сервису)
├── CLOUD_MODELS_MIGRATION.md
├── MODEL_PATHS_CONFIGURATION.md
└── ...                      # Другие релевантные README файлы
```

### Файлы, которые НЕ переносятся:
- `frontend/` - это другой репозиторий
- `backend/` - это другой репозиторий
- `models/paraphrase-multilingual-MiniLM-L12-v2/` - модель эмбеддингов идет в backend
- `docker-compose-backend.yml` - другой сервис
- `docker-compose-frontend.yml` - другой сервис
- `docker-compose.yml` - монолитный файл, не нужен
- Все файлы, связанные с фронтендом или бэкендом

---

## 🔄 Общие файлы и настройки

### Файлы, которые могут понадобиться в нескольких репозиториях:

#### `.gitignore`
- Каждый репозиторий должен иметь свой `.gitignore`
- Используйте существующие `.gitignore` из соответствующих директорий
- Для `llm_svc` - используйте `llm-svc/.gitignore`
- Для `frontend` - используйте `frontend/.gitignore`
- Для `backend` - создайте новый или используйте корневой `.gitignore`

#### `.env.example`
- Каждый репозиторий должен иметь свой `.env.example`
- `llm-svc/env.example` → `llm_svc/.env.example`
- `backend/env.example` → `backend/.env.example`
- Создайте `frontend/.env.example` если нужен

#### `README.md`
- Каждый репозиторий должен иметь свой основной `README.md`
- Используйте существующие README из соответствующих директорий
- Добавьте инструкции по запуску через Docker Compose

---

## 📋 Чеклист переноса

### Для репозитория `frontend`:
- [ ] Скопировать `frontend/` директорию
- [ ] Скопировать `docker-compose-frontend.yml`
- [ ] Скопировать `assets/` директорию
- [ ] Проверить `nginx.conf` (если используется отдельный)
- [ ] Создать `.env.example` для фронтенда
- [ ] Обновить `README.md` с инструкциями по запуску
- [ ] Проверить, что `package.json` содержит все зависимости

### Для репозитория `backend`:
- [ ] Скопировать `backend/` директорию
- [ ] Скопировать `docker-compose-backend.yml`
- [ ] Скопировать `nginx.conf`
- [ ] Скопировать `install_pgvector_extension.sql`
- [ ] Скопировать `context_prompts.json` (если используется)
- [ ] Создать пустые директории `memory/` и `uploads/` (или с `.gitkeep`)
- [ ] Скопировать модель эмбеддингов `models/paraphrase-multilingual-MiniLM-L12-v2/` (если используется)
- [ ] Создать `.env.example` на основе `backend/env.example`
- [ ] Обновить `README.md` с инструкциями по запуску
- [ ] Скопировать релевантные файлы из `README/` директории

### Для репозитория `llm_svc`:
- [ ] Скопировать `llm-svc/` директорию
- [ ] Скопировать `docker-compose-llm-svc.yml`
- [ ] Скопировать все модели из `models/` (кроме paraphrase-multilingual)
- [ ] Скопировать `model_small/` (Vosk модели)
- [ ] Скопировать `silero_models/`
- [ ] Скопировать `whisperx_models/`
- [ ] Скопировать `diarize_models/`
- [ ] Создать `.env.example` на основе `llm-svc/env.example`
- [ ] Обновить `README.md` с инструкциями по запуску
- [ ] Скопировать релевантные файлы из `README/` директории

---

## 🚀 После переноса

### Настройка Docker сети

После переноса в отдельные репозитории, для работы микросервисов вместе нужно:

1. **Создать общую Docker сеть** (выполнить один раз):
   ```bash
   docker network create astrachat-network
   ```

2. **Обновить docker-compose файлы** - установить `external: true` для сети:
   ```yaml
   networks:
     astrachat-network:
       external: true
   ```

3. **Порядок запуска сервисов**:
   ```bash
   # 1. Запустить LLM сервис
   cd llm_svc
   docker-compose -f docker-compose-llm-svc.yml up -d
   
   # 2. Запустить Backend
   cd ../backend
   docker-compose -f docker-compose-backend.yml up -d
   
   # 3. Запустить Frontend
   cd ../frontend
   docker-compose -f docker-compose-frontend.yml up -d
   ```

### Обновление путей в конфигурациях

После переноса проверьте и обновите:
- Пути к моделям в `docker-compose-llm-svc.yml`
- Пути к volumes в `docker-compose-backend.yml`
- URL для API в `frontend` (переменные окружения)
- Сетевые имена сервисов в nginx конфигурациях

---

## ⚠️ Важные замечания

1. **Модели занимают много места** - рассмотрите использование Git LFS или отдельного хранилища для моделей
2. **Переменные окружения** - каждый репозиторий должен иметь свой `.env.example`
3. **Docker сеть** - для работы вместе сервисы должны быть в одной Docker сети
4. **Volumes** - данные БД и MinIO должны быть доступны из backend репозитория
5. **Зависимости** - backend зависит от llm-svc, frontend зависит от backend

---

## 📝 Дополнительные файлы (не переносятся никуда)

Эти файлы остаются в исходном репозитории или удаляются:
- `docker-compose.yml` - монолитный файл, заменен на три отдельных
- `main.py` - корневой файл (если не используется)
- `switch_to_llm_svc.py` - утилита для переключения (если не нужна)
- `restart_backend.py` - утилита (если не нужна)
- `check_minio.py` - утилита диагностики (можно удалить или оставить в backend)
- `start_minio.bat`, `run_minio.bat` - скрипты для Windows (если не нужны)
- `start_with_llm_svc.bat` - скрипт запуска (если не нужен)
- `venv_312/`, `myvenv/`, `node_modules/` - виртуальные окружения (не коммитить)
- `memoai.spec` - спецификация для PyInstaller (если не используется)
- Документация в корне (`*.md`, `*.docx`, `*.xlsx`, `*.pptx`) - решить индивидуально
- `README/` - документация, распределить по репозиториям по релевантности

---

**Дата создания:** 2024
**Версия:** 1.0

